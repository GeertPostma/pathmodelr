library(pls)

get_LVs <- function(nodes, connection_matrix){ #Add options for different methods (SO-PLS, Multicomponent regression) in addition to just 1-1
  

  #Default mode: predict all upstream blocks from the block that is being estimated. Start with setting end-node LV's to the raw data
  
  
  #TODO: Add other modes
}